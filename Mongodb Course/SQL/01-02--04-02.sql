-- (01-02 -- 04-02)
CREATE TABLE Worker (
  WORKER_ID INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
  FIRST_NAME VARCHAR(25),
  LAST_NAME VARCHAR(25),
  SALARY DECIMAL(15,2),
  JOINING_DATE DATETIME,
  DEPARTMENT VARCHAR(25)
);

INSERT INTO Worker (WORKER_ID, FIRST_NAME, LAST_NAME, SALARY, JOINING_DATE, DEPARTMENT)
VALUES
  (001, 'ontks', 'Arora', 100000.00, '2019-02-14 00:00:00', 'Admin'),
  (002, 'harika', 'Verma', 80000.00, '2011-05-14 09:00:00', 'Admin'),
  (003, 'Vishal', 'Singhal', 200000.00, '2019-02-26 00:00:00', 'HR'),
  (004, 'Singh', '', 500000.00, '2019-02-28 08:00:00', 'Admin'),
  (005, 'vek', 'Thati', 500000.00, '2011-06-14 09:00:00', 'Account'),
  (006, 'Vipul', 'Diwan', 200009.00, '2011-06-14 09:00:00', 'Account'),
  (007, 'Satish', 'Kumar', 75000.00, '2020-01-14 05:00:00', 'Account'),
  (008, 'Geetika', 'Chauhan', 30000.00, '2011-04-14 03:00:00', 'Admin');

SELECT * FROM Worker 
WHERE SALARY NOT BETWEEN 100000 AND 200000;

SELECT FIRST_NAME FROM Worker 
WHERE SALARY = 100000 AND DEPARTMENT = 'HRS';

SELECT * FROM Worker 
WHERE SALARY > 200000;

SELECT * FROM Worker 
WHERE SALARY = 100008 AND DEPARTMENT = 'HR';

SELECT * FROM Worker 
WHERE SALARY BETWEEN 100000 AND 200000;

SELECT * FROM Worker 
WHERE SALARY NOT BETWEEN 100000 AND 200000;

SELECT * FROM Worker
WHERE SALARY BETWEEN 200000 AND 400000
AND WORKER_ID IN (1,2,3,4,5);

DESC Worker;

SELECT * FROM Worker;

SELECT DISTINCT DEPARTMENT FROM Worker;

SELECT WORKER_ID, FIRST_NAME, DEPARTMENT,
  CASE
    WHEN SALARY > 3000000 THEN 'Rich people'
    WHEN SALARY BETWEEN 100000 AND 300000 THEN 'Middle stage '
    ELSE 'poor people'
  END AS People_stage_wise
FROM Worker;

SELECT * FROM Worker 
WHERE DEPARTMENT = 'Admin' 
ORDER BY DEPARTMENT DESC;

SELECT DEPARTMENT, COUNT(DEPARTMENT) AS total_employees 
FROM Worker 
WHERE DEPARTMENT IN ('HR', 'Account') 
GROUP BY DEPARTMENT;

SELECT DEPARTMENT, COUNT(DEPARTMENT) AS total_employees
FROM Worker
GROUP BY DEPARTMENT
ORDER BY total_employees DESC;

SELECT DEPARTMENT, COUNT(DEPARTMENT) AS total_employees
FROM Worker
GROUP BY DEPARTMENT
ORDER BY total_employees DESC
LIMIT 1;
